{% extends 'base.admin.html.twig' %}
{% block body %}
    <div class="list-group">
        <span class="list-group-item active text-center">
                {{ app.request.get('referential') }}
        </span>
        <a href="{{ url('admin_referential_edit', {'referential': app.request.get('referential')}) }}"
           class="list-group-item list-group-item-action">
            Edition
        </a>
        <a href="{{ url('admin_referential_details', {'referential': app.request.get('referential')}) }}"
           class="list-group-item list-group-item-action">
            Détails
        </a>
        <div class="list-group-item list-group-item-action">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}